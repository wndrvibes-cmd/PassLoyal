# hooks/ — réservé pour les hooks React de la future application SaaS

Vide pour l'instant. Accueillera les hooks partagés par le dashboard (ex. `useAuth`, `useSupabaseQuery`, `useWalletStatus`) une fois l'application développée. Le site vitrine n'a besoin d'aucun hook custom à ce jour — sa logique est trop simple pour le justifier.
