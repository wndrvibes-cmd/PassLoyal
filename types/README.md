# types/ — réservé pour les types partagés de la future application SaaS

Vide pour l'instant. Accueillera les types de domaine partagés entre `app/(dashboard)/`, `services/` et `components/` (ex. `Customer`, `LoyaltyCard`, `Reward`, `Subscription`) une fois l'application développée. Les types du site vitrine restent colocalisés avec leur contenu dans `lib/content/*.ts` — pas besoin de les déplacer ici.
